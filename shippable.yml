language: ruby
build:
  cache: true
  pre_ci_boot:
    image_name: ric03uec/u16ruball
    image_tag: v6.2.4
  ci:
    - . /etc/profile.d/rvm.sh
    - rvm version
    - rvm get master
    - rvm version
    - echo "hello world"
    - env
    - source $BASH_ENV
    - rvm list
